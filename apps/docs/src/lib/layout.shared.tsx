import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";

export function baseOptions(): BaseLayoutProps {
  return {
    nav: {
      title: "React Native Scenes",
      transparentMode: "top",
    },
    githubUrl: 'https://github.com/Rezrazi/react-native-scenes',
  };
}

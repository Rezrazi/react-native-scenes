{
  "name": "@react-native-scenes/maestro-flows",
  "description": "Maestro flows used for testing and demo purposes",
  "scripts": {
    "start": "maestro test --test-output-dir ./output ./flows",
    "poststart": "bun run clean:results && bun run videos:process && bun run videos:upload && bun run screenshots:move && bun run clean:output",
    "videos:process": "bun run scripts/process-videos.ts",
    "videos:upload": "bun run scripts/upload-videos.ts",
    "screenshots:move": "bun run scripts/move-screenshots.ts",
    "clean:results": "rm -rf output/2025-*",
    "clean:output": "rm -f output/**/*.mp4 output/**/*.png"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "@vercel/blob": "^0.27.0",
    "bun-types": "latest"
  }
}
